# offline-pdf-rag-chatbot
Offline PDF RAG chatbot built with LangChain, FAISS, and HuggingFace Transformers. Loads PDFs, creates embeddings, and answers user queries locally without API keys. Simple command‑line interface for document Q&amp;A, ideal for learning retrieval‑augmented generation.
